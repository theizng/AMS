<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AMS.Views.PaymentsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Chu kỳ thanh toán">

    <!-- Toolbar mirrors Maintenance layout style -->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Tạo chu kỳ" Priority="0" Order="Primary" Command="{Binding CreateCycleCommand}" />
        <ToolbarItem Text="Đồng bộ phòng" Priority="1" Order="Primary" Command="{Binding ReseedCommand}" />
        <ToolbarItem Text="Tính trạng thái" Priority="2" Order="Primary" Command="{Binding RecomputeCommand}" />
        <ToolbarItem Text="Gửi tất cả có hóa đơn" Priority="3" Order="Secondary" Command="{Binding SendAllWithInvoiceCommand}" />
    </ContentPage.ToolbarItems>

    <Grid Padding="12" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Cycle selector -->
        <HorizontalStackLayout Grid.Row="0" Spacing="8">
            <Picker 
                Title="Chọn chu kỳ"
                WidthRequest="200"
                ItemsSource="{Binding Cycles}"
                ItemDisplayBinding="{Binding Display}"
                SelectedItem="{Binding SelectedCycle}" />
        </HorizontalStackLayout>

        <!-- Row 1: KPIs + status -->
        <VerticalStackLayout Grid.Row="1" Spacing="8">
            <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="8">
                <Frame Padding="8" CornerRadius="8" BorderColor="#CFD8DC">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Tổng phải thu" FontAttributes="Bold"/>
                        <Label Text="{Binding TotalDue, StringFormat='{0:N0} đ'}" />
                    </VerticalStackLayout>
                </Frame>
                <Frame Padding="8" CornerRadius="8" BorderColor="#CFD8DC">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Đã thu" FontAttributes="Bold"/>
                        <Label Text="{Binding TotalPaid, StringFormat='{0:N0} đ'}" />
                    </VerticalStackLayout>
                </Frame>
                <Frame Padding="8" CornerRadius="8" BorderColor="#CFD8DC">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Còn lại" FontAttributes="Bold"/>
                        <!-- FIX: bind to TotalRemaining (viewmodel property) -->
                        <Label Text="{Binding TotalRemaining, StringFormat='{0:N0} đ'}" />
                    </VerticalStackLayout>
                </Frame>
                <Frame Padding="8" CornerRadius="8" BorderColor="#CFD8DC">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Sẵn sàng gửi" FontAttributes="Bold"/>
                        <Label Text="{Binding ReadyCount}" />
                    </VerticalStackLayout>
                </Frame>
            </Grid>

            <Label
                Text="{Binding StatusMessage}"
                TextColor="#FF7043"
                FontSize="12"
                IsVisible="{Binding StatusMessage, Converter={StaticResource StringNotEmptyConverter}}" />
        </VerticalStackLayout>

        <!-- Row 2: List -->
        <CollectionView
            Grid.Row="2"
            ItemsSource="{Binding Rows}"
            SelectionMode="None">

            <CollectionView.EmptyView>
                <Grid Padding="20">
                    <Label
                        Text="Chưa có dữ liệu cho chu kỳ đã chọn."
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        TextColor="#666" />
                </Grid>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame
                        Margin="0,6"
                        Padding="12"
                        HasShadow="True"
                        BorderColor="#E0E0E0"
                        CornerRadius="10">
                        <Grid ColumnSpacing="12" RowSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Row 1: Room + Status badge (right) -->
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding RoomCode}"
                                   FontAttributes="Bold" />

                            <Border Grid.Row="0" Grid.Column="1"
                                    Padding="8,4" StrokeThickness="0" Background="#EEE"
                                    HorizontalOptions="End" VerticalOptions="Start">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8" />
                                </Border.StrokeShape>
                                <!-- Vietnamese status text bound from OverviewRow.StatusText -->
                                <Label Text="{Binding StatusText}" />
                            </Border>

                            <!-- Row 2: Summary -->
                            <Label Grid.Row="1" Grid.ColumnSpan="2"
                                   Text="{Binding Summary}"
                                   LineBreakMode="TailTruncation"
                                   MaxLines="4" />

                            <!-- Row 3: Left spacer, Right = actions -->
                            <Grid Grid.Row="2" Grid.ColumnSpan="2"
                                  ColumnDefinitions="*,Auto" ColumnSpacing="12">
                                <Grid Grid.Column="0" /> <!-- spacer or future messages -->

                                <VerticalStackLayout Grid.Column="1" Spacing="6" HorizontalOptions="End">
                                    <!-- Single consolidated button -->
                                    <Button Text="Cập nhật trạng thái"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ChooseStatusCommand}"
                                            CommandParameter="{Binding .}" />
                                    <Button Text="Gửi hóa đơn (PDF)"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SendInvoiceEmailCommand}"
                                            CommandParameter="{Binding .}" />
                                </VerticalStackLayout>
                            </Grid>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>