<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AMS.Views.TenantsPage"
             Title="Người thuê">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Thêm" Priority="0" Order="Primary" Command="{Binding AddTenantCommand}" />
        <ToolbarItem Text="Làm mới" Priority="1" Order="Primary" Command="{Binding RefreshCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <ResourceDictionary>
            <conv:BoolToColorConverter x:Key="BoolToColorConverter" xmlns:conv="clr-namespace:AMS.Converters" />
            <conv:InvertBoolConverter x:Key="InvertBoolConverter" xmlns:conv="clr-namespace:AMS.Converters" />
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <Grid RowDefinitions="Auto,Auto,*" Padding="10" RowSpacing="10">
        <!-- Search -->
        <HorizontalStackLayout Spacing="8">
            <Entry Placeholder="Tìm theo tên, SĐT, email, CCCD/CMND..."
                   Text="{Binding SearchText}" HorizontalOptions="FillAndExpand" />
            <Button Text="Tìm" Command="{Binding SearchCommand}" />
        </HorizontalStackLayout>

        <!-- Status filter -->
        <HorizontalStackLayout Grid.Row="1" Spacing="8">
            <Label Text="Trạng thái:" VerticalOptions="Center" />
            <Picker ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" />
            <Button Text="Xóa lọc" Command="{Binding ClearFilterCommand}" />
        </HorizontalStackLayout>

        <RefreshView Grid.Row="2"
                     IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}">
            <CollectionView ItemsSource="{Binding Tenants}" SelectionMode="None">
                <CollectionView.EmptyView>
                    <Grid Padding="20">
                        <Label Text="Chưa có người thuê nào. Nhấn 'Thêm' để tạo mới."
                               HorizontalOptions="Center" VerticalOptions="Center" TextColor="#666"/>
                    </Grid>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Style="{DynamicResource Card}">
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="12">
                                <VerticalStackLayout Grid.Column="0" Spacing="4">
                                    <HorizontalStackLayout Spacing="8">
                                        <Label Text="{Binding FullName}" FontSize="18" FontAttributes="Bold" Style="{DynamicResource TextPrimary}" />
                                        <Border Style="{DynamicResource Chip}">
                                            <Label Text="{Binding IsActive, StringFormat='{0:Hoạt động;Ngừng}'}"
                   TextColor="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}" />
                                        </Border>
                                    </HorizontalStackLayout>

                                    <Label Text="{Binding PhoneNumber, StringFormat='SĐT: {0}'}" Style="{DynamicResource TextPrimary}" />
                                    <Label Text="{Binding IdCardNumber, StringFormat='CCCD/CMND: {0}'}" Style="{DynamicResource TextPrimary}" />
                                    <Label Text="{Binding Email, StringFormat='Email: {0}'}" Style="{DynamicResource TextPrimary}" />
                                    <Label Text="{Binding PermanentAddress, StringFormat='Địa chỉ: {0}'}" Style="{DynamicResource TextPrimary}" />
                                    <Label Text="{Binding EmergencyContactsText, StringFormat='Người liên hệ khẩn cấp: {0}'}" Style="{DynamicResource TextSecondary}" />
                                    <Label Text="{Binding CurrentRoomDisplay, StringFormat='Đang thuê phòng: {0}'}" Style="{DynamicResource TextSecondary}" />
                                    <Label Text="{Binding ActiveBikeCount, StringFormat='Số lượng xe máy: {0}'}" Style="{DynamicResource TextSecondary}" />
                                </VerticalStackLayout>

                                <VerticalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">
                                    <Button Text="Sửa"
                
                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditTenantCommand}"
                CommandParameter="{Binding .}" />
                                    <Button Text="Hủy kích hoạt"
                IsVisible="{Binding IsActive}"
                Style="{DynamicResource DangerButton}"
                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeactivateCommand}"
                CommandParameter="{Binding .}" />
                                    <Button Text="Kích hoạt"
                IsVisible="{Binding IsActive, Converter={StaticResource InvertBoolConverter}}"
                Style="{DynamicResource SuccessButton}"
                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ActivateCommand}"
                CommandParameter="{Binding .}" />
                                    <Button Text="Xóa"
                Style="{DynamicResource DangerButton}"
                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteTenantCommand}"
                CommandParameter="{Binding .}" />
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>